#include<stdio.h>
#include<string.h>
void main()
{
   int a,b,c,d;
   printf("Enter four numbers : ");
   scanf("%d%d%d%d",&a,&b,&c,&d);
   int big=a>b && b>c && c>d ? a : ((b>c && c>d) ? b: (c>d)?c:d);
   
   printf("%d",big);

}
